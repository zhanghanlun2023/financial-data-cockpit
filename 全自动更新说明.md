# 财务驾驶舱全自动更新

## 使用方法

1. 将财务月度数据文件放入 `待处理数据` 文件夹。
2. 文件名包含“行业”“对标”“基准”或 `benchmark` 时，系统按行业对标数据处理；其他文件按公司财务月度数据处理。
3. 自动任务每 5 分钟扫描一次。
4. 校验通过后，系统自动备份旧数据、写入标准 CSV、提交并推送 GitHub；Streamlit Cloud 随后自动重新部署。
5. 成功文件移动到 `已处理`，失败文件移动到 `处理失败`，详细原因记录在 `日志`。

## 安全规则

- 字段缺失、日期无法识别、关键金额为空、日期与公司重复等情况不会上线。
- 更新前自动备份当前数据。
- 自动提交仅包含看板标准数据文件，不会提交原始财务文件、日志或备份。
- 财务数据请使用 `财务月度数据模板.xlsx`，行业数据请使用 `行业对标数据模板.xlsx`。

## 手动运行一次

```powershell
python auto_update.py
```

## 只更新本地、不推送 GitHub

```powershell
python auto_update.py --no-publish
```
